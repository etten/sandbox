; Custom php.ini values for PHP as CGI/FCGI/FPM.
; If you use PHP as Apache module, configure it another way (web custom php.ini, .htaccess).

memory_limit = 128M
max_execution_time = 60

upload_max_filesize = 100M
post_max_size = 100M
