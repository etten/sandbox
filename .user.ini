; Custom php.ini values for PHP as CGI/FCGI/FPM.
; Don't forget set the same also in .htaccess (for PHP as Apache module).

memory_limit = 128M
max_execution_time = 60

upload_max_filesize = 100M
post_max_size = 100M
