parameters:


application:
	# This prevents DIC rebuilds on local (development).
	scanDirs: no
	scanComposer: no


kdyby.doctrine:
	user: #<database-user>
	password: #<database-password>
	dbname: #<database-name>
